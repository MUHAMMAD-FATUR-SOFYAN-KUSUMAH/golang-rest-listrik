	// rows, _ := tranction.ExecContext(context.Background(), sqlpub)
	// result, _ := rows.LastInsertId()
	// fmt.Println(result)
	// temp := domain.User{}
	// row.Scan(&temp.Username, &temp.Name, &temp.Role_level.Id_level, &temp.Role_level.Nama_level, &temp.Level)
	// fmt.Println(temp.Username, temp.Name, temp.Role_level.Id_level, temp.Role_level.Nama_level)
	// defer tranction.Commit()
	str := "@mimix"
	fmt.Println(str[0])

}
